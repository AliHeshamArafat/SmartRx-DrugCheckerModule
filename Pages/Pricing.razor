@page "/pricing"
@using SmartRx_DrugChecker.Services
@implements IDisposable
@inject MarketStateService MarketState

<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&display=swap" rel="stylesheet">

<div class="pricing-page">
    <!-- Hero Section -->
    <section class="pricing-hero">
        <h1 class="pricing-hero-title">From Safer Prescriptions To Smarter Care</h1>
        <p class="pricing-hero-subtitle">AI-powered drug interaction checking for individuals, clinics, and hospitals.</p>
    </section>

    <!-- Pricing Plans Section -->
    <section class="pricing-plans-section">
        <!-- Toggle Switch -->
        <div class="pricing-toggle-container">
            <button class="pricing-toggle @(isMonthly ? "active" : "")" @onclick='() => SetBillingPeriod(true)'>
                Monthly
            </button>
            <button class="pricing-toggle @(!isMonthly ? "active" : "")" @onclick='() => SetBillingPeriod(false)'>
                Annual
            </button>
        </div>

        <!-- Pricing Cards -->
        <div class="pricing-cards-container">
            <!-- Individual Plan -->
            <div class="pricing-card">
                <div class="pricing-card-header">
                    <div class="pricing-card-title-row">
                        <h3 class="pricing-card-title">Individual</h3>
                        <div class="popular-badge-placeholder"></div>
                    </div>
                    <p class="pricing-card-subtitle">For doctors & pharmacists</p>
                </div>
                <div class="pricing-card-price">
                    <div class="price-content">
                        <span class="price-currency">@GetCurrencySymbol()</span>
                        <span class="price-amount">@GetPriceNumber("individual")</span>
                        <span class="price-period">@(isMonthly ? "/mo" : "/yr")</span>
                    </div>
                    @if (!isMonthly)
                    {
                        <span class="save-badge">Save 34%</span>
                    }
                </div>
                <ul class="pricing-features">
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>1 user</span>
                    </li>
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Access on up to 2 devices</span>
                    </li>
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Core drug interaction check</span>
                    </li>
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Basic alerts</span>
                    </li>
                </ul>
                <button class="pricing-card-button outline" @onclick='() => NavigateToPayment("individual")'>Get Started</button>
            </div>

            <!-- Team Plan (Most Popular) -->
            <div class="pricing-card featured">
                <div class="pricing-card-header">
                    <div class="pricing-card-title-row">
                        <h3 class="pricing-card-title">Team</h3>
                        <div class="popular-badge">Most Popular</div>
                    </div>
                    <p class="pricing-card-subtitle">For small clinics & pharmacies</p>
                </div>
                <div class="pricing-card-price">
                    <div class="price-content">
                        <span class="price-currency">@GetCurrencySymbol()</span>
                        <span class="price-amount">@GetPriceNumber("team")</span>
                        <span class="price-period">@(isMonthly ? "/mo" : "/yr")</span>
                    </div>
                    @if (!isMonthly)
                    {
                        <span class="save-badge">Save 34%</span>
                    }
                </div>
                <ul class="pricing-features">
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Up to 10 users</span>
                    </li>
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Shared drug history</span>
                    </li>
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Advanced interaction analysis</span>
                    </li>
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Priority support</span>
                    </li>
                </ul>
                <button class="pricing-card-button primary" @onclick='() => NavigateToPayment("team")'>Choose Team</button>
            </div>

            <!-- Clinic Pro Plan -->
            <div class="pricing-card">
                <div class="pricing-card-header">
                    <div class="pricing-card-title-row">
                        <h3 class="pricing-card-title">Clinic Pro</h3>
                        <div class="popular-badge-placeholder"></div>
                    </div>
                    <p class="pricing-card-subtitle">For medical centers</p>
                </div>
                <div class="pricing-card-price">
                    <div class="price-content">
                        <span class="price-currency">@GetCurrencySymbol()</span>
                        <span class="price-amount">@GetPriceNumber("clinic")</span>
                        <span class="price-period">@(isMonthly ? "/mo" : "/yr")</span>
                    </div>
                    @if (!isMonthly)
                    {
                        <span class="save-badge">Save 34%</span>
                    }
                </div>
                <ul class="pricing-features">
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>25-50 users</span>
                    </li>
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>User roles & permissions</span>
                    </li>
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Reports & audit logs</span>
                    </li>
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Full drug database access</span>
                    </li>
                </ul>
                <button class="pricing-card-button outline" @onclick='() => NavigateToPayment("clinic")'>Get Started</button>
            </div>

            <!-- Enterprise Plan -->
            <div class="pricing-card">
                <div class="pricing-card-header">
                    <div class="pricing-card-title-row">
                        <h3 class="pricing-card-title">Enterprise</h3>
                        <div class="popular-badge-placeholder"></div>
                    </div>
                    <p class="pricing-card-subtitle">For hospitals & large organizations</p>
                </div>
                <div class="pricing-card-price">
                    <span class="price-amount custom-pricing">Custom pricing</span>
                </div>
                <ul class="pricing-features">
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Unlimited users</span>
                    </li>
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>System integrations (HIS/EMR)</span>
                    </li>
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Custom rules & SLA</span>
                    </li>
                    <li class="pricing-feature">
                        <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
                            <path d="M20 6L9 17L4 12" stroke="#4994FF" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Dedicated support</span>
                    </li>
                </ul>
                <button class="pricing-card-button outline">Talk to Sales</button>
            </div>
        </div>
    </section>

    <!-- Trusted by Partners Section -->
    <section class="trusted-section">
        <div class="trusted-container">
            <h2 class="trusted-title">Trusted by healthcare partners</h2>
            <p class="trusted-subtitle">Part of the SmartRx healthcare ecosystem</p>
            <div class="partners-grid">
                <div class="partner-item">
                    <div class="partner-logo">HP</div>
                    <p class="partner-text">Partner Name</p>
                </div>
                <div class="partner-item">
                    <div class="partner-logo">M</div>
                    <p class="partner-text">Partner Name</p>
                </div>
                <div class="partner-item">
                    <div class="partner-logo">CH</div>
                    <p class="partner-text">Partner Name</p>
                </div>
                <div class="partner-item">
                    <div class="partner-logo">P</div>
                    <p class="partner-text">Partner Name</p>
                </div>
                <div class="partner-item">
                    <div class="partner-logo">MG</div>
                    <p class="partner-text">Partner Name</p>
                </div>
                <div class="partner-item">
                    <div class="partner-logo">WC</div>
                    <p class="partner-text">Partner Name</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Bottom CTA Section -->
    <section class="pricing-cta-section">
        <h2 class="cta-title">Need help choosing the right plan?</h2>
        <p class="cta-subtitle">Our team is here to help you find the perfect solution for your practice or organization.</p>
        <button class="cta-button">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
                <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
        </button>
    </section>
</div>

<style>
    .pricing-page {
        min-height: 100vh;
        font-family: 'Montserrat', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background: transparent;
        position: relative;
    }

    /* Hero Section */
    .pricing-hero {
        text-align: center;
        padding: 4rem 2rem 3rem;
        max-width: 1200px;
        margin: 0 auto;
    }

    .pricing-hero-title {
        font-size: 52px;
        font-weight: 700;
        color: #4994FF;
        margin: 0 auto 1rem auto;
        line-height: 1.2;
        max-width: 700px;
    }

    .pricing-hero-subtitle {
        font-size: 18px;
        color: #000000;
        margin: 0 auto;
        font-weight: 400;
        max-width: 700px;
    }

    /* Pricing Plans Section */
    .pricing-plans-section {
        max-width: 1400px;
        margin: 0 auto;
        padding: 3rem 2rem;
    }

    .pricing-toggle-container {
        display: flex;
        justify-content: center;
        gap: 0;
        margin-bottom: 3rem;
        background: #F3F4F6;
        border-radius: 12px;
        padding: 4px;
        width: fit-content;
        margin-left: auto;
        margin-right: auto;
    }

    .pricing-toggle {
        padding: 0.75rem 2rem;
        border: none;
        background: transparent;
        color: #4994FF;
        font-size: 1rem;
        font-weight: 500;
        cursor: pointer;
        border-radius: 8px;
        transition: all 0.3s ease;
        font-family: 'Montserrat', sans-serif;
    }

    .pricing-toggle.active {
        background: #4994FF;
        color: white;
    }

    .pricing-cards-container {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(302px, 1fr));
        gap: 2rem;
        margin-top: 2rem;
        justify-items: center;
    }

    .pricing-card {
        background: transparent;
        backdrop-filter: blur(2px);
        -webkit-backdrop-filter: blur(2px);
        border: 1.5px solid rgba(255, 255, 255, 0.3);
        border-radius: 16px;
        padding: 2rem 1rem 1rem 2rem;
        padding-bottom: 5.5rem;
        position: relative;
        transition: all 0.3s ease;
        width: 302px;
        height: 378px;
        display: flex;
        flex-direction: column;
        box-sizing: border-box;
        box-shadow: 
            0 8px 32px rgba(0, 0, 0, 0.1),
            0 2px 8px rgba(0, 0, 0, 0.05),
            inset 0 1px 0 rgba(255, 255, 255, 0.6);
    }

    .pricing-card:hover {
        transform: translateY(-4px);
        border-color: #4994FF;
        border-width: 1.5px;
        box-shadow: 
            0 12px 40px rgba(73, 148, 255, 0.15),
            0 4px 12px rgba(0, 0, 0, 0.08),
            inset 0 1px 0 rgba(255, 255, 255, 0.6);
    }

    .pricing-card.featured {
        border: 1.5px solid rgba(255, 255, 255, 0.3);
    }

    .pricing-card.featured:hover {
        transform: translateY(-4px);
        border-color: #4994FF;
        border-width: 1.5px;
        box-shadow: 
            0 12px 40px rgba(73, 148, 255, 0.15),
            0 4px 12px rgba(0, 0, 0, 0.08),
            inset 0 1px 0 rgba(255, 255, 255, 0.6);
    }

    .pricing-card-title-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 0.5rem;
        margin-bottom: 0.5rem;
        min-height: 28px;
    }

    .popular-badge {
        background: #4994FF;
        color: white;
        padding: 0.25rem 0.75rem;
        border-radius: 5px;
        font-size: 0.75rem;
        font-weight: 600;
        white-space: nowrap;
        height: fit-content;
    }

    .popular-badge-placeholder {
        width: auto;
        height: auto;
        visibility: hidden;
        padding: 0.25rem 0.75rem;
        font-size: 0.75rem;
    }

    .pricing-card-header {
        margin-bottom: 0.5rem;
        min-height: 64px;
    }

    .pricing-card-title {
        font-size: 20px;
        font-weight: 700;
        color: #111827;
        margin: 0;
        line-height: 28px;
    }

    .pricing-card-subtitle {
        font-size: 14px;
        color: #6B7280;
        margin: 0;
        line-height: 20px;
        margin-top: 0.5rem;
    }

    .pricing-card-price {
        margin-bottom: 1.5rem;
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        gap: 0.5rem;
        min-height: 40px;
    }

    .price-content {
        display: flex;
        align-items: baseline;
        gap: 0.25rem;
        flex: 1;
    }

    .price-currency {
        font-size: 36px;
        font-weight: 700;
        color: #111827;
        line-height: 40px;
        display: inline-flex;
        align-items: center;
    }

    .price-currency img {
        width: 1em;
        height: 1em;
        object-fit: contain;
    }

    .save-badge {
        background-color: rgba(255, 176, 80, 0.1);
        color: #FFB050;
        padding: 0.25rem 0.75rem;
        border-radius: 5px;
        font-size: 0.75rem;
        font-weight: 400;
        width: fit-content;
        white-space: nowrap;
    }

    .price-amount {
        font-size: 36px;
        font-weight: 700;
        color: #111827;
        line-height: 40px;
    }

    .price-amount.custom-pricing {
        font-size: 24px;
        line-height: 40px;
    }

    .price-period {
        font-size: 14px;
        color: #6B7280;
        line-height: 20px;
    }

    .pricing-features {
        list-style: none;
        padding: 0;
        margin: 0 0 auto 0;
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
        flex: 1;
        min-height: 0;
    }

    .pricing-feature {
        display: flex;
        align-items: flex-start;
        gap: 0.75rem;
        font-size: 14px;
        color: #000000;
    }

    .check-icon {
        flex-shrink: 0;
        margin-top: 2px;
    }

    .pricing-card-button {
        width: 242px;
        height: 50px;
        border-radius: 10px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        font-family: 'Montserrat', sans-serif;
        border: 1.5px solid #D1D5DC;
        background: transparent;
        color: #111827;
        position: absolute;
        bottom: 2rem;
        left: 50%;
        transform: translateX(-50%);
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .pricing-card-button:hover {
        background: #4994FF;
        color: white;
        border-color: #4994FF;
    }

    .pricing-card-button.primary {
        background: transparent;
        color: #111827;
        border-color: #D1D5DC;
    }

    .pricing-card-button.primary:hover {
        background: #4994FF;
        color: white;
        border-color: #4994FF;
    }

    .pricing-card-button.outline {
        background: transparent;
        color: #111827;
        border-color: #D1D5DC;
    }

    .pricing-card-button.outline:hover {
        background: #4994FF;
        color: white;
        border-color: #4994FF;
    }

    /* Trusted Section */
    .trusted-section {
        padding: 3rem 2rem;
        max-width: 1400px;
        margin: 0 auto;
    }

    .trusted-container {
        background: transparent;
        backdrop-filter: blur(2px);
        -webkit-backdrop-filter: blur(2px);
        border-radius: 16px;
        border: 1px solid rgba(255, 255, 255, 0.3);
        padding: 3rem 2rem;
        text-align: center;
        box-shadow: 
            0 8px 32px rgba(0, 0, 0, 0.1),
            0 2px 8px rgba(0, 0, 0, 0.05),
            inset 0 1px 0 rgba(255, 255, 255, 0.6);
        transition: all 0.3s ease;
    }

    .trusted-container:hover {
        transform: translateY(-2px);
        box-shadow: 
            0 12px 40px rgba(0, 0, 0, 0.12),
            0 4px 12px rgba(0, 0, 0, 0.08),
            inset 0 1px 0 rgba(255, 255, 255, 0.6);
    }

    .trusted-title {
        font-size: 24px;
        font-weight: 700;
        color: #4994FF;
        margin: 0 auto 1rem auto;
        max-width: 390px;
        line-height: 32px;
    }

    .trusted-subtitle {
        font-size: 14px;
        line-height: 20px;
        color: #000000;
        margin: 0 auto 2rem auto;
        font-weight: 400;
    }

    .partners-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(80px, 1fr));
        gap: 2rem;
        max-width: 800px;
        margin: 0 auto;
        justify-items: center;
    }

    .partner-item {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0.5rem;
    }

    .partner-logo {
        background: #D1D5DC;
        border-radius: 4px;
        width: 80px;
        height: 48px;
        font-size: 24px;
        font-weight: 700;
        color: #99A1AF;
        display: flex;
        align-items: center;
        justify-content: center;
        line-height: 32px;
    }

    .partner-text {
        font-size: 12px;
        line-height: 16px;
        font-weight: 500;
        color: #99A1AF;
        margin: 0;
    }

    /* CTA Section */
    .pricing-cta-section {
        text-align: center;
        padding: 4rem 2rem;
        max-width: 800px;
        margin: 0 auto;
    }

    .cta-title {
        font-size: 64px;
        font-weight: 700;
        color: #1A73E8;
        margin: 0 auto 1rem auto;
        line-height: 70px;
        max-width: 670px;
        text-align: center;
    }

    .cta-subtitle {
        font-size: 15px;
        line-height: 32px;
        color: #4D4D4D;
        margin: 0 auto 2rem auto;
        max-width: 400px;
        text-align: center;
    }

    .cta-button {
        width: 48px;
        height: 48px;
        border-radius: 50%;
        background: #F3F4F6;
        border: none;
        cursor: pointer;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        color: #6B7280;
        transition: all 0.3s ease;
    }

    .cta-button:hover {
        background: #E5E7EB;
        color: #1A73E8;
    }

    @@media (max-width: 1440px) {
        .pricing-card {
            width: 100%;
            max-width: 302px;
            height: auto;
            min-height: 378px;
        }
    }

    @@media (max-width: 1440px) {
        .trusted-container {
            padding: 2.5rem 1.5rem;
        }

        .trusted-title {
            font-size: 22px;
            line-height: 30px;
        }

        .partners-grid {
            gap: 1.5rem;
        }

        .partner-logo {
            width: 70px;
            height: 42px;
            font-size: 22px;
            line-height: 28px;
        }
    }

    @@media (max-width: 768px) {
        .pricing-page {
            padding-top: 100px;
        }

        .pricing-hero-title {
            font-size: 32px;
        }

        .pricing-hero-subtitle {
            font-size: 16px;
        }

        .pricing-cards-container {
            grid-template-columns: 1fr;
            justify-items: center;
        }

        .pricing-card {
            width: 100%;
            max-width: 302px;
            height: auto;
            min-height: 378px;
        }

        .pricing-toggle-container {
            width: 100%;
            max-width: 300px;
        }

        .pricing-toggle {
            flex: 1;
        }

        .trusted-section {
            padding: 2rem 1rem;
        }

        .trusted-container {
            padding: 2rem 1.5rem;
        }

        .trusted-title {
            font-size: 20px;
            line-height: 28px;
            max-width: 100%;
        }

        .trusted-subtitle {
            font-size: 13px;
            line-height: 18px;
        }

        .partners-grid {
            grid-template-columns: repeat(3, 1fr);
            gap: 1rem;
        }

        .partner-logo {
            width: 60px;
            height: 36px;
            font-size: 20px;
            line-height: 26px;
        }

        .partner-text {
            font-size: 11px;
            line-height: 14px;
        }

        .cta-title {
            font-size: 36px;
            line-height: 42px;
            max-width: 100%;
        }

        .cta-subtitle {
            font-size: 14px;
            line-height: 24px;
            max-width: 100%;
        }
    }

    @@media (max-width: 1200px) {
        .cta-title {
            font-size: 48px;
            line-height: 54px;
        }

        .cta-subtitle {
            font-size: 14px;
            line-height: 28px;
        }
    }

    @@media (max-width: 992px) {
        .cta-title {
            font-size: 40px;
            line-height: 46px;
        }
    }
</style>

@code {
    [Inject]
    private NavigationManager? Navigation { get; set; }

    private bool isMonthly = true;

    protected override void OnInitialized()
    {
        if (MarketState != null)
        {
            MarketState.OnMarketChanged += HandleMarketChanged;
        }
    }

    private void NavigateToPayment(string plan)
    {
        if (Navigation != null)
        {
            Navigation.NavigateTo($"/payment/{plan}");
        }
    }

    public void Dispose()
    {
        if (MarketState != null)
        {
            MarketState.OnMarketChanged -= HandleMarketChanged;
        }
    }

    private void HandleMarketChanged()
    {
        InvokeAsync(StateHasChanged);
    }

    private void SetBillingPeriod(bool monthly)
    {
        isMonthly = monthly;
        StateHasChanged();
    }

    private string GetPriceNumber(string plan)
    {
        var market = MarketState?.SelectedMarket ?? "egypt";
        var prices = GetPricingForMarket(market);
        
        if (prices.ContainsKey(plan))
        {
            var basePrice = prices[plan];
            if (!isMonthly && plan != "enterprise")
            {
                // Annual pricing (10 months price for 12 months)
                basePrice = (int)(basePrice * 10);
            }
            return basePrice.ToString();
        }
        
        return "Custom";
    }

    private RenderFragment GetCurrencySymbol()
    {
        var market = MarketState?.SelectedMarket ?? "egypt";
        
        if (market == "egypt")
        {
            return builder =>
            {
                builder.AddContent(0, "£");
            };
        }
        else // saudi
        {
            return builder =>
            {
                builder.OpenElement(0, "img");
                builder.AddAttribute(1, "src", "/images/Riyal.webp");
                builder.AddAttribute(2, "alt", "SAR");
                builder.CloseElement();
            };
        }
    }

    private Dictionary<string, int> GetPricingForMarket(string market)
    {
        if (market == "egypt")
        {
            return new Dictionary<string, int>
            {
                { "individual", 29 },
                { "team", 99 },
                { "clinic", 299 }
            };
        }
        else // saudi
        {
            return new Dictionary<string, int>
            {
                { "individual", 120 },
                { "team", 400 },
                { "clinic", 1200 }
            };
        }
    }
}
